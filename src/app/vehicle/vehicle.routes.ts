import { Route } from '@angular/router';

import { VehicleShellComponent } from './components/vehicle-shell/vehicle-shell.component';

export const routes: Route[] = [
  {
    path: '',
    component: VehicleShellComponent,
  },
];
